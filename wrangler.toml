name = "corranzo"
compatibility_date = "2026-06-24"
main = "worker/index.js"

# Worker runs first, serves real dist/ files via ASSETS, then index.html for SPA routes.
[assets]
directory = "./dist"
binding = "ASSETS"
run_worker_first = true
