/**
 * Automatic practice tracking is disabled. Practice time is recorded only when
 * the user saves a session from the manual practice log on Profile.
 */
export default function usePracticeStatsTracker() {}
